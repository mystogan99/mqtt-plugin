import { ControllerProps } from './types';
declare const Controller: ({ name, rules, as: InnerComponent, onChange, onBlur, onChangeName, onBlurName, valueName, defaultValue, control, ...rest }: ControllerProps) => JSX.Element;
export { Controller };
