declare class GitHubRelease {
    token: string;
    username: string;
    repository: string;
    releaseNotes: string;
    commitHash?: string;
    constructor(token: string, username: string, repository: string, releaseNotes: string, commitHash?: string);
    /**
     * Get the ghr binary to perform the release
     */
    private getGhr;
    release(recreate: boolean): Promise<void>;
}
export { GitHubRelease };
