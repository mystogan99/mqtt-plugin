/// <reference types="react" />
import { DataFrame } from '@grafana/data';
interface TransformationRowProps {
    name: string;
    description: string;
    editor?: JSX.Element;
    onRemove: () => void;
    input: DataFrame[];
}
export declare const TransformationRow: ({ onRemove, editor, name, input }: TransformationRowProps) => JSX.Element;
export {};
