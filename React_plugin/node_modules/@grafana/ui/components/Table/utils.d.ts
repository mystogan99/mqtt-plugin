import { TextAlignProperty } from 'csstype';
import { DataFrame, Field } from '@grafana/data';
import { Column } from 'react-table';
import { TableRow } from './types';
export declare function getTableRows(data: DataFrame): TableRow[];
export declare function getTextAlign(field: Field): TextAlignProperty;
export declare function getColumns(data: DataFrame, availableWidth: number, columnMinWidth: number): Column[];
