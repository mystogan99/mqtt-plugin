import { FC } from 'react';
import { FormInputSize } from '../Forms/types';
interface Props {
    value: string;
    size?: FormInputSize;
    onChange: (newValue: string) => void;
}
export declare const TimeZonePicker: FC<Props>;
export {};
