import { FC } from 'react';
import { ButtonSize } from '../Button/types';
interface Props {
    size?: ButtonSize;
    disabled?: boolean;
    onConfirm(): void;
}
export declare const DeleteButton: FC<Props>;
export {};
