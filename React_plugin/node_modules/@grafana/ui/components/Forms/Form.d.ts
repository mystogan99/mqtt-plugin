import React from 'react';
import { Mode, OnSubmit, DeepPartial, FormContextValues } from 'react-hook-form';
declare type FormAPI<T> = Pick<FormContextValues<T>, 'register' | 'errors' | 'control'>;
interface FormProps<T> {
    validateOn?: Mode;
    defaultValues?: DeepPartial<T>;
    onSubmit: OnSubmit<T>;
    children: (api: FormAPI<T>) => React.ReactNode;
}
export declare function Form<T>({ defaultValues, onSubmit, children, validateOn }: FormProps<T>): JSX.Element;
export {};
