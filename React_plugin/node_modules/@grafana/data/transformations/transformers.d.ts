import { DataFrame } from '../types/dataFrame';
import { Registry } from '../utils/Registry';
import { AppendOptions } from './transformers/append';
import { ReduceTransformerOptions } from './transformers/reduce';
import { FilterFieldsByNameTransformerOptions } from './transformers/filterByName';
import { DataTransformerInfo, DataTransformerConfig } from '../types/transformations';
/**
 * Apply configured transformations to the input data
 */
export declare function transformDataFrame(options: DataTransformerConfig[], data: DataFrame[]): DataFrame[];
/**
 * Registry of transformation options that can be driven by
 * stored configuration files.
 */
declare class TransformerRegistry extends Registry<DataTransformerInfo> {
    append(data: DataFrame[], options?: AppendOptions): DataFrame | undefined;
    reduce(data: DataFrame[], options: ReduceTransformerOptions): DataFrame[];
}
export declare const transformersRegistry: TransformerRegistry;
export { ReduceTransformerOptions, FilterFieldsByNameTransformerOptions };
