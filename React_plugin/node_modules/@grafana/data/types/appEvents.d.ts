export interface AppEvent<T> {
    readonly name: string;
    payload?: T;
}
export declare namespace AppEvents {
    type AlertPayload = [string, string?];
    const alertSuccess: AppEvent<AlertPayload>;
    const alertWarning: AppEvent<AlertPayload>;
    const alertError: AppEvent<AlertPayload>;
}
